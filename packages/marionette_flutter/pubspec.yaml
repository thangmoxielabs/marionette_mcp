name: marionette_flutter
description: Flutter development extensions for VM service integration, providing runtime widget inspection and interaction capabilities for AI agents.
version: 0.1.0
repository: https://github.com/leancodepl/marionette
issue_tracker: https://github.com/leancodepl/marionette/issues

environment:
  sdk: "^3.8.1"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
