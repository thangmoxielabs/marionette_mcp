name: marionette_mcp_server
description: MCP server for Flutter app interaction - enables AI agents to inspect and interact with running Flutter apps via VM service.
version: 0.1.0
repository: https://github.com/leancodepl/marionette
issue_tracker: https://github.com/leancodepl/marionette/issues

executables:
  marionette_mcp_server: marionette_mcp_server

environment:
  sdk: ^3.6.1

dependencies:
  args: ^2.7.0
  logging: ^1.3.0
  mcp_dart: ^0.6.4
  vm_service: ^15.0.0

dev_dependencies:
  lints: ^6.0.0
